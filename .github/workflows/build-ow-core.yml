name: build-ow-core
on:
  push:
  schedule:
    - cron: '0 2 * * 0'
  workflow_dispatch:

env:
  HDF5_VERSION: "1.14.6"

jobs:
  build-linux:
    runs-on: ubuntu-latest
    env:
      STATIC_HDF5: "1"
      HDF5_INSTALL_DIR: /home/runner/hdf5-install
    steps:
        - uses: actions/checkout@v4
        - name: Cache HDF5
          uses: actions/cache@v4
          with:
            path: ${{ env.HDF5_INSTALL_DIR }}
            key: hdf5-${{ env.HDF5_VERSION }}-linux-threadsafe-v1
        - run: ./build/ci/all.sh
        - name: upload build
          uses: actions/upload-artifact@v4
          with:
            name: linux-${{ github.sha }}
            path: |
              ow-inspect
              ow-single
              ow-sim
              ows-ensemble
              libopenwater.h
              libopenwater.so
        - name: Zip nightly
          run: printf "ow-inspect\now-single\now-sim\nows-ensemble\nlibopenwater.h\nlibopenwater.so" | zip build.zip -@
        - name: Rename for versioned release
          if: github.ref_type == 'tag'
          run: mv build.zip build-linux.zip
        - name: Release Nightly
          if: github.ref_type != 'tag'
          uses: softprops/action-gh-release@v1
          with:
            name: ubuntu-Nightly
            files: build.zip
            fail_on_unmatched_files: true
            tag_name: Nightly-ubuntu
        - name: Release Version
          if: github.ref_type == 'tag'
          uses: softprops/action-gh-release@v1
          with:
            name: ${{ github.ref_name }}
            files: build-linux.zip
            fail_on_unmatched_files: true
            tag_name: ${{ github.ref_name }}
            draft: false
            prerelease: false
  build-windows:
    runs-on: windows-latest
    steps:
        - uses: actions/checkout@v4
        - name: Cache HDF5
          uses: actions/cache@v4
          with:
            path: hdf5-install
            key: hdf5-${{ env.HDF5_VERSION }}-windows-threadsafe-v1
        - name: build
          run: ./build/ci/all.sh
          shell: bash
        - name: upload build
          uses: actions/upload-artifact@v4
          with:
            name: windows-${{ github.sha }}
            path: |
              *.exe
              hdf5-dlls/*.dll
              libopenwater.h
              libopenwater.dll
        - name: Zip Nightly
          run: |
            mkdir -p build_temp
            cp ~/go/bin/ow-inspect.exe build_temp/
            cp ~/go/bin/ow-single.exe build_temp/
            cp ~/go/bin/ow-sim.exe build_temp/
            cp ~/go/bin/ows-ensemble.exe build_temp/
            cp ../bin/libopenwater.h build_temp/
            cp ../bin/libopenwater.dll build_temp/
            cp hdf5-dlls/*.dll build_temp/
            7z a -tzip build.zip ./build_temp/*
          shell: bash
        - name: Rename for versioned release
          if: github.ref_type == 'tag'
          run: mv build.zip build-windows.zip
          shell: bash
        - name: Release Nightly
          if: github.ref_type != 'tag'
          uses: softprops/action-gh-release@v1
          with:
            name: windows-Nightly
            files: build.zip
            fail_on_unmatched_files: true
            tag_name: Nightly-windows
        - name: Release Version
          if: github.ref_type == 'tag'
          uses: softprops/action-gh-release@v1
          with:
            name: ${{ github.ref_name }}
            files: build-windows.zip
            fail_on_unmatched_files: true
            tag_name: ${{ github.ref_name }}
            draft: false
            prerelease: false
  build-osx:
    runs-on: macos-latest
    env:
      HDF5_INSTALL_DIR: /Users/runner/hdf5-install
    steps:
      - uses: actions/checkout@v4
      - name: Cache HDF5
        uses: actions/cache@v4
        with:
          path: ${{ env.HDF5_INSTALL_DIR }}
          key: hdf5-${{ env.HDF5_VERSION }}-macos-threadsafe-v1
      - name: build
        run: ./build/ci/all.sh
      - name: upload build
        uses: actions/upload-artifact@v4
        with:
          name: macos-${{ github.sha }}
          path: |
            ow-inspect
            ow-single
            ow-sim
            ows-ensemble
            libopenwater.h
            libopenwater.dylib
      - name: Zip nightly
        run: printf "ow-inspect\now-single\now-sim\nows-ensemble\nlibopenwater.h\nlibopenwater.dylib" | zip build.zip -@
      - name: Rename for versioned release
        if: github.ref_type == 'tag'
        run: mv build.zip build-macos.zip
      - name: Release Nightly
        if: github.ref_type != 'tag'
        uses: softprops/action-gh-release@v1
        with:
          name: osx-Nightly
          files: build.zip
          fail_on_unmatched_files: true
          tag_name: Nightly-osx
      - name: Release Version
        if: github.ref_type == 'tag'
        uses: softprops/action-gh-release@v1
        with:
          name: ${{ github.ref_name }}
          files: build-macos.zip
          fail_on_unmatched_files: true
          tag_name: ${{ github.ref_name }}
          draft: false
          prerelease: false
