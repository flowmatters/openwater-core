language: go
sudo: required

go:
  - 1.8.x

before_install:
  - sudo apt-get update
  - sudo apt-get install -y libhdf5-serial-dev python3-venv

script:
  - ls
  - go test -v ./...
  - ls
  - python3 --version
  - ./build/bootstrap-test-env.sh
